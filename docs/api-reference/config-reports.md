# Config Reports

Configuration report APIs collect nested config objects into a structured
document, render that document into supported output formats, and allow custom
renderer registration.

::: themis.config_report
    options:
      show_root_heading: false
