# Types

Shared enums, event payloads, and JSON/value helpers used by specs, records, and
storage.

`themis.types` is the stable namespace for shared enums and event/value models.
It is lazy-loaded so optional dependencies stay out of the base import path
until they are needed.

::: themis.types
    options:
      show_root_heading: false

::: themis.types.enums
    options:
      show_root_heading: false

::: themis.types.events
    options:
      show_root_heading: false
