# Telemetry

In-process telemetry primitives and the optional Langfuse callback integration.

Use these APIs when you want to subscribe to runtime events, forward them to an
external tracing backend, or persist external trace links back into timeline
views.

::: themis.telemetry.bus
    options:
      show_root_heading: false

::: themis.telemetry.langfuse_callback
    options:
      show_root_heading: false
