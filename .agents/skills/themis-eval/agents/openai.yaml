interface:
  display_name: "Themis Eval"
  short_description: "Build, inspect, and monitor Themis eval workflows"
  default_prompt: "Use $themis-eval to build, inspect, monitor, or debug a themis-eval experiment workflow."
